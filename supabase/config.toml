project_id = "ein-mehr-beissen-bitte"

[functions.recipe]
# Require a valid Supabase JWT (the anon key qualifies). Keeps the proxy tied to
# this project rather than being open to the entire internet.
verify_jwt = true
